import 'package:flutter/material.dart';

class CongregationsScreen extends StatelessWidget {
  const CongregationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Congregações - Em desenvolvimento'),
    );
  }
}